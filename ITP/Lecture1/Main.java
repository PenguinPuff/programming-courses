package de.tum.cit.ase;

public class Main {
    public static void main(String[] args) {
        /*
        Door frontLeft = new Door();
        Door frontRight = new Door();
        Door rearLeft = new Door();
        Door rearRight = new Door();

        // Adjust the arguments of the constructors to the implementation of the classes (will be changed while working
        // on the problem)
        Seal driver = new Seal("Vincent", (short) 6, 190, true);
        Engine engine = new Engine(240, null);

        Car car = new Car("1", engine, frontLeft, frontRight, rearLeft, rearRight);
        car.setDriver(driver);
        engine.setCar(car);
         */
    }
}
